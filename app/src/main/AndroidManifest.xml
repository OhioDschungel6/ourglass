<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.othregensburg.ourglass">

    <uses-permission android:name="android.permission.NFC" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">



        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER"/>

            </intent-filter>
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/com.othregensburg.ourglass" />
            </intent-filter>

        </activity>
        <activity
            android:name=".TagesuebersichtActivity"
            android:label="@string/title_activity_tagesuebersicht"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Teamuebersicht"
            android:label="@string/title_activity_teamuebersicht"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Stundenuebersicht"
            android:label="@string/title_activity_stundenuebersicht"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Projektuebersicht"
            android:label="@string/title_activity_projektuebersicht"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Stundenkorrektur"
            android:label="@string/title_activity_stundenkorrektur"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Startseite"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            >
        </activity>
        <activity android:name=".FirstLogin" />
    </application>

</manifest>